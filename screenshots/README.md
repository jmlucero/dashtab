# Screenshots

Put your captures here so the main README can show them:

- `dashboard.png` — a new tab with a few tickers and notes loaded.
- `settings.png` — the Settings panel open.

On macOS, `Cmd+Shift+4` lets you capture a region of the screen.
